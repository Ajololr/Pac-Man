program Pac_Man;

{$R *.dres}

uses
  System.StartUpCopy,
  FMX.Forms,
  MainMenu in 'MainMenu.pas' {StartMenu};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TStartMenu, StartMenu);
  Application.Run;
end.
