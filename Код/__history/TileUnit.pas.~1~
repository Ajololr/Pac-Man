unit TileUnit;

interface

type
    Coordinat = record
        X: Real;
        Y: Real;
    end;

    Tile = class
        Wall: Boolean;
        Dot: Boolean;
        BigDot: Boolean;
        Eaten: Boolean;
        Pos: Coordinat;
        constructor Create(X, Y: Real);
        procedure Show();
    end;

implementation

constructor Create(X, Y: Real);
begin
    New(Tile);
    Tile.Pos.X := X;
    Tile.Pos.Y := Y;
end;

procedure Show();
begin
    if Tile.Dot then

end;

end.
